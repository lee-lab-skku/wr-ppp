% Repository version: v1.3.0-beta
\documentclass[10pt,a4paper]{article}
\usepackage{weekly-report}

\begin{document}

% This is an adaptable example of a weekly research report.
% Keep the focus on research within two A4 pages per person per week.
% See the repository README.md, "Write the Report" and "Length and Exceptions",
% for the full report policy and the narrow exception to the page limit.
% Adapt sections, subsections, and grouping to the week's actual research.
% Choose prose, lists, equations, tables, and figures to suit the material.
% Lead with important takeaways and connect claims, support, and implications;
% these relationships need not form a fixed sequence or labels in every item.
% Replace illustrative content and keep only structures that help the reader.

% Inspect weekly-report.sty's package declarations and configuration first;
% use the loaded packages where they suit the report's content.
% Examples already available through the style:
% - cleveref references: \cref{tab:main-results}
%   or \cref{fig:calibration-curve,fig:rare-class-errors}.
% - siunitx units and quantities: \si{\metre\per\second}
%   or \SI{25}{\degreeCelsius}.
% - mhchem chemical notation: \ce{H2O} or \ce{2 H2 + O2 -> 2 H2O}.
% These are illustrative calls, not an exhaustive package or command list.
% Use other commands, argument forms, and options supported by the loaded
% package versions as appropriate; do not add content just to use an example.

% =========================================================
% Report information -- edit these three values.
% Choose a title that reflects the scope of the week's research,
% whether it concerns one finding or several research threads.
% =========================================================
\ReportHeader
    {Calibration-Aware Classification in the Low-Data Regime}
    {Example Author}
    {Demo Study}


% =========================================================
% Abstract
%
% Orient the reader to the week's scope and significant developments.
% Synthesize what matters across threads as needed, using material supported
% by the body. Include unresolved issues or next directions when they help.
% =========================================================
\begin{reportabstract}

Adding a mild calibration penalty improved macro F1 by 2.3 percentage
points while reducing calibration error. The gain was consistent in
four of five random seeds, but the rarest class remains unstable. Next
week's work will therefore focus on label quality and seed sensitivity.

\end{reportabstract}


\vspace{3mm}


% =========================================================
% P1. Progress
%
% Explain what changed in the research or understanding and why it matters,
% with evidence or reasoning appropriate to the claim. Give important
% takeaways prominence and keep supporting details subordinate to them.
% Progress need not be a positive result or a completed experiment.
% =========================================================
\begin{pppbox}{Progress}

\ReportSubsection{Moderate Penalty Improves F1 and Calibration}

A moderate calibration penalty improved macro F1 by 2.3 percentage points
and reduced ECE from 8.7 to 5.1 across five seeds
(\cref{tab:main-results}). Data splits and preprocessing were held fixed.

% Use a comparison table when it helps the reader assess the evidence.
% Choose columns and captions that make the comparison interpretable;
% explain its research significance in the surrounding discussion as needed.
\ReportTable
    {L C C C}
    {
        \toprule
        Method & Macro F1 $\uparrow$ & ECE $\downarrow$ & Worst-class F1 $\uparrow$ \\
        \midrule
        Baseline & 71.4 & 8.7 & 48.2 \\
        Calibration penalty & \textbf{73.7} & 5.1 & \textbf{52.6} \\
        Strong penalty & 72.8 & \textbf{3.9} & 49.7 \\
        \bottomrule
    }
    {Illustrative validation results averaged over five random seeds.}
    {tab:main-results}

The moderate penalty therefore offers the best observed balance for
follow-up validation. The stronger penalty minimized ECE but lost part
of the rare-class gain, so lower ECE alone does not establish a better model.


\ReportSubsection{Penalty Strength Limits the Gain}

The sweep suggests that calibration strength must remain modest:
$\lambda=0.05$ improved calibration without reducing accuracy, whereas
larger values produced under-confident predictions. The tested loss was

\[
    \mathcal{L}
    = \mathcal{L}_{\mathrm{CE}}
    + \frac{\lambda}{N}
      \sum_{i=1}^{N}\left\lVert\mathbf{p}_i-\mathbf{y}_i\right\rVert_2^2.
\]

The effect was strongest near class boundaries, motivating a label audit
before attributing the rare-class gain to the loss formulation.

\end{pppbox}


% Use paired figures when seeing both helps explain the evidence or reasoning.
% Choose informative captions and connect the displays to the discussion.
% Missing files render as placeholders; replace them or omit unused figures.
\ReportFigurePair
    {figures/calibration-curve.pdf}
    {Calibration curves for the baseline and revised loss.}
    {fig:calibration-curve}
    {figures/rare-class-errors.pdf}
    {Error composition for the rarest validation class.}
    {fig:rare-class-errors}


\vspace{2mm}


% =========================================================
% P2. Problems
%
% Make relevant unresolved issues and their research consequences clear.
% Choose hypotheses, attempted responses, or needed evidence and input
% according to the actual issue; these prompts are not compulsory fields.
% Distinguish observations from proposed explanations.
% =========================================================
\begin{pppbox}{Problems}

\ReportSubsection{Rare-Class Instability}

Worst-class F1 still varied by 7.8 points across seeds, so the apparent
gain is not yet strong enough to support a general conclusion.

\begin{itemize}
    \item \textbf{Working hypothesis:} a small cluster of inconsistently
          labeled samples changes the decision boundary between splits.
    \item \textbf{Attempts and evidence:} class weighting reduced mean
          performance, while manual inspection found three ambiguous samples.
    \item \textbf{Next response:} audit the rare-class labels and repeat the
          comparison with stratified seeds and confidence intervals.
\end{itemize}


\ReportSubsection{Primary Metric Requires a Decision}

The primary metric remains undecided: macro F1 measures class balance,
whereas ECE measures calibration. Advisor feedback on the deployment
priority will determine the main claim; both metrics will remain reported.

\end{pppbox}


% Keep a standalone figure when it communicates necessary support more clearly
% than prose alone; omit a display that only repeats another presentation.
\ReportFigure
    {figures/seed-variance.pdf}
    {Worst-class F1 across random seeds; error bars should be included in the final analysis.}
    {fig:seed-variance}
    {30mm}


\vspace{2mm}


% =========================================================
% P3. Plans
%
% Explain the next research direction and intended learning or outcome.
% Choose a level of detail suited to the state of the work. Priorities,
% success criteria, dependencies, or fallbacks can clarify the direction
% when relevant, without requiring a fixed set of fields for every plan.
% =========================================================
\begin{pppbox}{Plans}

\ReportSubsection{Validation and Decision Sequence}

The next priority is to determine whether label quality explains
rare-class instability.

% A planning table is optional; choose it when comparing actions is useful.
% Adapt its columns and level of detail to the plans being communicated.
\ReportTable
    {c L c}
    {
        \toprule
        Priority & Task & Expected Output \\
        \midrule
        High & Audit rare-class labels with a second reviewer. & Reviewed label set \\
        High & Repeat five-seed evaluation after the audit. & Confidence intervals \\
        Medium & Compare macro F1 and ECE as primary metrics. & Recommendation note \\
        \bottomrule
    }
    {Planned work for the next reporting period.}
    {tab:next-week}

If the label audit cannot be completed, the fallback is to run the seed
study on the current labels and mark label quality as an explicit limitation.
The week is successful if the instability is either reduced or explained.

\end{pppbox}


\vfill

\end{document}
